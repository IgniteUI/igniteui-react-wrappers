(function (factory) {
	if (typeof define === "function" && define.amd) {

		// AMD. Register as an anonymous module.
		define( [
			"react",
			"jquery"
		], factory );
	} else {

		// Browser globals
		factory(React, jQuery);
	}
}
(function(React, $) {
